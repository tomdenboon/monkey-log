import { createTheme } from "./utils";

const defaultTheme = createTheme({
  primary: "#f3f4f6",
  primaryLight: "#ffffff",
  secondary: "#FFA500",
  textBase: "#374151",
  textLight: "#737a87",
  textLightest: "#9ca3af",
  warning: "#ee4444",
  success: "#34d399",
  successLight: "#dcfce7",
});

export default defaultTheme;
